/**
 * Google OAuth 2.0 Client ID for "Sign in with Google" (greeting name).
 * Replace the placeholder with your own Client ID — see README
 * "Google Sign-In setup" for the exact Google Cloud Console steps.
 *
 * This is a public OAuth client ID (not a secret) so committing it is fine,
 * but it's per-extension-install: the authorized redirect URI depends on
 * this extension's ID, which differs between your Brave/Firefox installs.
 */
export const GOOGLE_CLIENT_ID = 'YOUR_CLIENT_ID.apps.googleusercontent.com';
